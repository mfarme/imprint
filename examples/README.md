# Example layout

Use this folder for a small completed example showing how a real capability is represented. Keep examples generic and replace them with project-specific content when adopting the template.

Suggested example:

```text
domain/
└── accounts/
    ├── intent.md
    ├── invariants.md
    ├── scenarios/
    │   ├── create-account.md
    │   └── duplicate-email.md
    └── decisions.md
```
